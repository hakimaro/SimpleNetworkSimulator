#include <iostream>
#include "modulation.h"

using namespace std;

int main()
{
//    int MCS_item_size = sizeof(LTE::MCS);
//    int MCS_size = sizeof(LTE::MCS_table);
//    for (int i = 0; i < MCS_size / MCS_item_size; ++i) {
//        cout << "Modulation: " << (int) LTE::MCS_table[i].modulation << " | TBS: " << LTE::MCS_table[i].TBS_index << endl;
//    }

    start_modulation(100);

    return 0;
}
